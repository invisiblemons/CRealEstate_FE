import { Location } from '../Location/location-class';
import { Viewport } from '../Viewport/viewport-class';

export class GeometryAddress {
  location: Location;
  location_type: string;
  viewport: Viewport;
}
